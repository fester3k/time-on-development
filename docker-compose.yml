services:
  cycle-monitor:
    build: ./service
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - cycle-data:/data
    environment:
      - DB_PATH=/data/cycle.db

volumes:
  cycle-data:
